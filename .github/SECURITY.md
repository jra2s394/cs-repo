# Security Policy

The full security policy lives at [`SECURITY.md`](../SECURITY.md) in the repository root.

GitHub's community-profile detector also checks this location, so this file exists as a pointer. Edit the root file, not this one — the root version is authoritative.
